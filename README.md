Used Segment Anything model(SAM) for segmentation and YoloV8 for dedicated segmentation
In SAM we cannot classify the segmentation therefore we used yolo to pass dedicated area for segmentation to SAM
